<nav class='light-blue'>
  <div class='nav-wrapper container'>
    <div class='col s12 truncate'>
      <a href='/' class='breadcrumb'>Top</a>
      <a href='/teams/<%= @entry['team_id'] %>' class='breadcrumb'><%= @entry['team_name'] %></a>
      <a href='/entries/<%= @entry['entry_id'] %>' class='breadcrumb'><%= @entry['title'] %></a>
    </div>
  </div>
</nav>

<div class='container section'>
  <p><%= @entry['summary'] %></p>
  <p>登録者: <%= @entry['author_name'] %></p>
</div>

<% if @entry['author_id'] == current_user['id'] %>
  <div class='fixed-action-btn'>
    <a href='/entries/<%= @entry['entry_id'] %>/edit' class='btn-floating btn-large blue waves-effect waves-light'>
      <i class='material-icons'>mode_edit</i>
    </a>
  </div>
<% end %>
